import {StyleSheet} from "aphrodite/no-important";

// These styles are chosen to not violate Khan Academy trademarks.
const styles = StyleSheet.create({
    story: {
        background: "yellow",
        fontFamily: "Helvetica Neue, Helvetica, Arial, sans-serif",
        width: 520,
        fontWeight: 600,
        lineHeight: "48px",
        fontSize: 44,
        textAlign: "left",
        marginLeft: "auto",
        marginRight: "auto",
        marginTop: 64,
        position: "relative",
    },
});

export default styles;
